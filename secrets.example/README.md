# secrets.example

This folder documents the expected shape of the local `secrets/` folder.

The real `secrets/` folder is generated by:

```sh
./scripts/backup.sh
```

Never commit the real `secrets/` folder.
Keep it offline, preferably on a USB drive.
